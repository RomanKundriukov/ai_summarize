<?xml version="1.0" encoding="utf-8"?>
<winuiex:WindowEx
    MinHeight="650"
    MinWidth="370"
    xmlns:winuiex="using:WinUIEx"
    x:Class="ai_summarize.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ai_summarize"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:backdrop="using:ai_summarize.BackDrop"
    Title="ai_summarize">

    <winuiex:WindowEx.SystemBackdrop>
        <backdrop:BlurredBackdrop />
        <!--<winuiex:TransparentTintBackdrop />-->
    </winuiex:WindowEx.SystemBackdrop>


    <Frame Margin="5,35,5,5" x:Name="RootFrame"/>

    <!--<Grid RowSpacing="15" Margin="15,45,15,15" BackgroundSizing="OuterBorderEdge">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        --><!--<Rectangle Fill="{ThemeResource CustomAcrylicInAppBrush}" />--><!--

        <FlipView 
            Background="{ThemeResource CustomAcrylicInAppLuminosity}"
            MaxWidth="400" 
            Height="60" 
            BorderThickness="1" 
            ItemsSource="{x:Bind _vm.models, Mode=OneWay}">

            <FlipView.ItemTemplate>
                <DataTemplate x:DataType="ollama:OllamaModels">
                    <Grid>
                        <Border  Height="60" VerticalAlignment="Bottom">
                            <TextBlock Foreground="{ThemeResource TextFillColorPrimaryBrush}" Text="{x:Bind ModelName}" Padding="12,12" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>


        <TextBlock 
            Grid.Row="1"
            Text="File Drop"/>

        <TextBlock 
            Grid.Row="2"
            Text="Button?"/>
    </Grid>-->
</winuiex:WindowEx>
